ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [
      np.com.sudanchapagain.HttpKt.configureHttp
      np.com.sudanchapagain.RateLimitingKt.configureRateLimiting
      np.com.sudanchapagain.RoutingKt.configureRouting
    ]
  }
}